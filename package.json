{
  "name": "my-ncbi-proxy-server-only",
  "version": "1.0.0",
  "main": "server.js", // 这行可以保留，但 Vercel 不会使用它来启动 API Routes
  "type": "module", // 确保支持 ES modules
  "scripts": {
    "dev": "node server.js", // 本地开发时可能用到
    "build": "echo 'No build step needed for Vercel API Routes'" // Vercel API Routes 通常不需要构建
  },
  "dependencies": {
    "express": "^4.18.2", // 虽然 API Routes 不直接用 Express，但如果你的工具链需要它
    "cors": "^2.8.5",
    "axios": "^1.6.0",
    "jsdom": "^22.1.0"
  },
  "engines": {
    "node": ">=18" // 推荐指定 Node.js 版本
  }
}